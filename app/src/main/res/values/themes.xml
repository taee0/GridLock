<resources>
    <style name="Base.Theme.CoverScreen" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="colorPrimary">@color/fg</item>
    </style>

    <style name="Theme.CoverScreen" parent="Base.Theme.CoverScreen" />

    <!-- Used by the overlay windows on the cover panel. No decor, no dimming. -->
    <style name="Theme.CoverScreen.Overlay" parent="Base.Theme.CoverScreen">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- ConfigActivity. Translucent, no title, no animation, so when it is
         thrown onto the cover panel it does not flash a window frame first. -->
    <style name="Theme.CoverScreen.Config" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">always</item>
    </style>

    <!-- The rotation kicker. Must never be visible for a single frame. -->
    <style name="Theme.CoverScreen.Kick" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <!-- One row of the per app menu the recents icon chip opens. -->
    <style name="SheetItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/sheet_item</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">11dp</item>
        <item name="android:paddingBottom">11dp</item>
        <item name="android:textColor">@color/fg</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="LauncherTab">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:textColor">@color/dim</item>
        <item name="android:textSize">12sp</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
    </style>
    <style name="Theme.Launcher.Dark" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:textColorPrimary">@color/fg</item>
        <item name="android:textColorSecondary">@color/dim</item>
        <item name="android:colorAccent">@color/fg</item>
    </style>

    <style name="Theme.Launcher.Light" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFF7F7F7</item>
        <item name="android:colorBackground">#FFF7F7F7</item>
        <item name="android:textColorPrimary">#FF101010</item>
        <item name="android:textColorSecondary">#FF6B6B6B</item>
        <item name="android:colorAccent">#FF101010</item>
    </style>
</resources>
